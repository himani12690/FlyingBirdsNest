{
  "name": "nest-and-nosh",
  "private": true,
  "version": "1.0.0",
  "description": "Nest & Nosh — tiffin ordering app (static site + Playwright UI tests)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report tests/report",
    "setup": "npm install && npx playwright install chromium",
    "serve": "http-server -p 8080"   // <--- यह line जोड़ो
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "http-server": "^14.1.1"         // <--- यह line जोड़ो (ऊपर वाली के बाद कॉमा लगाना)
  }
}
